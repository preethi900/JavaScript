var generateTheString = function(n) {
    let str = '';
    if(n% 2 === 0) {
        for(var i= 0 ;i < n-1 ; i++) {
            str += 'a';
        }
        return str+'b';
    } else {
        for(var i= 0 ;i < n ; i++) {
            str += 'a';
        }
        return str;
    }
    
};
