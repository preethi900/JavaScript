var swapPairs = function(head) {
    if(!head || !head.next) {
        return head;
    }
    
        let temp = head.next;
        head.next = temp.next;
        temp.next = head;
        head.next = swapPairs(head.next);
    
    return temp;
};
