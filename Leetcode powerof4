/**
 * @param {number} num
 * @return {boolean}
 */
var isPowerOfFour = function(num) {
    if (num <= 0) {
        return false;
    }
    if (num == 1) {
        return true;
    }
    while (num % 4 === 0) {
        num /= 4;
    }
    return num === 1;
};
isPowerOfFour(4);
