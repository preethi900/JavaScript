/**
 * @param {string} s
 * @return {number}
 */
var countSegments = function(s) {
    s = s.replace(/\s+/g,' ').trim();
    var segment = s.split(" ");
    if (s === "") {
        return 0;
    }
    return segment.length;
    
    
};
countSegments("Hello");
