/**
 * @param {number[]} nums
 * @return {number[]}
 */
var findDisappearedNumbers = function(nums) {
   var arr2 =[];
    var nonarr = new Set(nums);
   
    for (var i = 1; i <= nums.length; i++) {
        if (!nonarr.has(i)) {
            arr2.push(i);            
        }
        
    }
   return arr2; 
};
