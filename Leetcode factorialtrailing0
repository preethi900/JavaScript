/**
 * @param {number} n
 * @return {number}
 */
var trailingZeroes = function(n) {
    
    var count = 0;
    while ( n > 4) {
        count += Math.floor(n /= 5);
        }
    return count;
    
    
    
};
trailingZeroes(5);
