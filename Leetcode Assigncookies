var findContentChildren = function(g, s) {
    g.sort((a,b) => a-b);
    s.sort((a,b) => a-b);
    let j=0;
    for (var i= 0;i< s.length ;i++) {
         if(g[j] <= s[i]) {
            j++;
        }
    }
    return j;
};
