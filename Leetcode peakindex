/**
 * @param {number[]} A
 * @return {number}
 */
var peakIndexInMountainArray = function(A) {
    if (A.length >= 3) {
        for (var i = 1 ; i < A.length - 1 ; i++) {
            if (A[i] > A[i+1]){
                return i;
            }
        }
    }
};
