/**
 * @param {string} s
 * @return {boolean}
 */
var repeatedSubstringPattern = function(s) {
    let n = s.length;
    let c = s + s;
  //  if (n % 2 !== 0) {
    //    return false;
   // }
    return c.slice(1,c.length - 1).includes(s);
     
};
