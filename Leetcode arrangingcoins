var arrangeCoins = function(n) {
    let count = n;
   for(var i=1 ; i <= n ; i++) {
       count -= i;
       if(count < 0) {
           return i-1;
       } else if(count === 0) {
           return i;
       }
   } 
    
};
