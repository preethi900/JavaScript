var xorOperation = function(n, start) {
    let nums = [];
    for(var i = 0 ; i < n ; i++) {
       nums[i] = start + (2*i); 
    }
    let count =nums[0];
    for(var i = 1 ; i < nums.length ; i++) {
       count  ^= nums[i];
    }
    return count;
    
};
