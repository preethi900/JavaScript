/**
 * @param {number} n
 * @return {number[]}
 */
var sumZero = function(n) {
    var arr = [];
    var num = -n +1;
    for (var i = 0 ; i < n ; i++) {
        arr.push(num);
        num = num + 2;
    }
    return arr;
};
