/**
 * @param {number[]} A
 * @return {number[]}
 */
var sortedSquares = function(A) {
    for (var i = 0; i < A.length; i++) {
        A[i] = Math.pow(A[i],2);
    }
    result = A.sort(function(a,b){return a - b});
    return result;
    
};
sortedSquares([-4,-1,0,3,10]);
