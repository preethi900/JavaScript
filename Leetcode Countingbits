/**
 * @param {number} num
 * @return {number[]}
 */
var countBits = function(num) {
    var arr = [];
    for (var i = 0 ; i <= num ; i++) {
        arr.push(Number(i).toString(2).replace(/0/g,'').length);
    }
    return arr;
};
