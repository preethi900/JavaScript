var preorderTraversal = function(root) {
    var visited = [];
    if(!root) {
        return visited;
    }
    traverse(root);
    function traverse(node) {
        visited.push(node.val);
        if(node.left) {
            traverse(node.left);
        }
        if(node.right) {
            traverse(node.right);
        }
    }
    return visited;
};
