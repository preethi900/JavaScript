var kthSmallest = function(matrix, k) {
    var arr =[];
    for (var i = 0 ;i < matrix.length ; i++) {
        for (var j =0 ; j < matrix[i].length ; j++) {
            arr.push(matrix[i][j]);
        }
    }
    arr.sort((a,b) =>a-b);
    return arr[k-1];
};
